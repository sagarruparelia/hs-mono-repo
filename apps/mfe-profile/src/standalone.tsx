/**
 * Standalone entry point for custom element
 * This ensures all dependencies are loaded properly
 */

// Import custom element (this will auto-register)
import './ce';

console.log('Standalone custom element bundle loaded');
